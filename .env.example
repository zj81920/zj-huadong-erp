# PostgreSQL (Docker) - 端口 5433，避免和本地 5432 冲突
DATABASE_URL="postgresql://<用户名>:<密码>@localhost:5433/huadong_erp?schema=public"

# 文件存储（本地开发阶段）
UPLOAD_DIR="./uploads"
STORAGE_MODE="local"

# 应用密钥（生产环境必须更换）
NEXTAUTH_SECRET="<替换为随机密钥>"
NEXTAUTH_URL="http://localhost:3000"
