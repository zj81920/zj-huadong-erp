# 共享 DetailCard 组件体系实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将审批页面和6+个模块页面中重复的24+处详情展示代码，提取为 `src/components/detail-cards/` 下的共享组件体系

**Architecture:** 每个模块一个纯展示型 DetailCard 组件，通过 DETAIL_CARD_MAP 注册中心统一暴露。模块页面和审批页面引用同一组件，各自在外层包装不同的操作按钮。

**Tech Stack:** Next.js 14 (App Router), React, Tailwind CSS, Vitest + React Testing Library + jsdom

---

### Task 1: 创建 DetailGrid 共享组件（TDD）

**Files:**
- Test: `test/unit/detail-cards-detail-grid.test.tsx`
- Create: `src/components/detail-cards/DetailGrid.tsx`

- [x] **Step 1: 编写 DetailGrid 测试**

```tsx
// test/unit/detail-cards-detail-grid.test.tsx
// @vitest-environment jsdom
import { describe, it, expect, afterEach } from 'vitest'
import { render, screen, cleanup } from '@testing-library/react'
import { DetailGrid } from '../../src/components/detail-cards/DetailGrid'

afterEach(() => { cleanup() })

describe('DetailGrid', () => {
  it('渲染指定数量的字段行（默认2列）', () => {
    render(
      <DetailGrid
        fields={[
          { label: '名称', value: '供应商A' },
          { label: '联系人', value: '张三' },
          { label: '电话', value: '13800138000' },
        ]}
      />
    )
    // 3个字段应有6个文本元素（3个label + 3个value）
    expect(screen.getByText('名称')).toBeDefined()
    expect(screen.getByText('供应商A')).toBeDefined()
    expect(screen.getByText('联系人')).toBeDefined()
    expect(screen.getByText('张三')).toBeDefined()
    expect(screen.getByText('电话')).toBeDefined()
    expect(screen.getByText('13800138000')).toBeDefined()
  })

  it('支持单列模式', () => {
    render(
      <DetailGrid
        fields={[
          { label: '名称', value: '供应商A' },
          { label: '描述', value: '这是一个很长的描述文本' },
        ]}
        columns={1}
      />
    )
    expect(screen.getByText('名称')).toBeDefined()
    expect(screen.getByText('供应商A')).toBeDefined()
  })

  it('值为空时渲染占位符', () => {
    render(
      <DetailGrid
        fields={[
          { label: '名称', value: '' },
          { label: '电话', value: null },
        ]}
      />
    )
    // 空值应渲染为 '-' 占位符
    const values = screen.getAllByText('-')
    expect(values.length).toBeGreaterThanOrEqual(2)
  })

  it('fields 为空数组时不渲染任何内容', () => {
    const { container } = render(<DetailGrid fields={[]} />)
    expect(container.firstChild).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/unit/detail-cards-detail-grid.test.tsx`
Expected: FAIL — 无法解析 `src/components/detail-cards/DetailGrid`

- [ ] **Step 3: 创建 DetailGrid 组件**

从 `src/app/(dashboard)/approvals/page.tsx` L780-791 提取，加上 columns 支持和空值处理：

```tsx
// src/components/detail-cards/DetailGrid.tsx
import React from 'react'

interface DetailGridProps {
  fields: { label: string; value: React.ReactNode }[]
  columns?: 1 | 2
}

export function DetailGrid({ fields, columns = 2 }: DetailGridProps) {
  if (fields.length === 0) return null

  return (
    <div className={`grid gap-x-8 gap-y-3 ${columns === 1 ? 'grid-cols-1' : 'grid-cols-2'}`}>
      {fields.map((field, index) => (
        <div key={index} className="flex flex-col gap-0.5">
          <span className="text-xs text-gray-500">{field.label}</span>
          <span className="text-sm text-gray-900">
            {field.value != null ? field.value : '-'}
          </span>
        </div>
      ))}
    </div>
  )
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/unit/detail-cards-detail-grid.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add test/unit/detail-cards-detail-grid.test.tsx src/components/detail-cards/DetailGrid.tsx
git commit -m "feat: 提取 DetailGrid 共享组件"
```

---

### Task 2: 创建 DETAIL_CARD_MAP 注册中心（TDD）

**Files:**
- Test: `test/unit/detail-cards-registration.test.ts`
- Create: `src/components/detail-cards/index.ts`（初始含供应商和收入/支出合同3个映射）
- Create: `src/components/detail-cards/suppliers/SupplierDetailCard.tsx`
- Create: `src/components/detail-cards/contracts/IncomeContractDetailCard.tsx`
- Create: `src/components/detail-cards/contracts/ExpenseContractDetailCard.tsx`

- [ ] **Step 1: 编写注册完整性测试**

```tsx
// test/unit/detail-cards-registration.test.ts
import { describe, it, expect } from 'vitest'
import { DETAIL_CARD_MAP } from '../../src/components/detail-cards'

describe('DETAIL_CARD_MAP 注册完整性', () => {
  const expectedBusinessTypes = [
    'supplier',
    'quotation',
    'outsourcing',
    'purchase_request',
    'delivery_receipt',
    'income_contract',
    'expense_contract',
    'non_contract_income',
    'non_contract_expense',
    'payment_application',
    'expense_report',
    'lending_out',
    'salary_payment',
    'borrowing_return_application',
    'other_borrowing',
    'inquiries',
    'inter_org_contract',
    'contract_change_order',
  ]

  it('所有业务类型都已注册', () => {
    for (const type of expectedBusinessTypes) {
      expect(DETAIL_CARD_MAP).toHaveProperty(type)
      expect(typeof DETAIL_CARD_MAP[type]).toBe('function')
    }
  })

  it('已注册的组件数量等于期望数量', () => {
    const registeredCount = Object.keys(DETAIL_CARD_MAP).length
    expect(registeredCount).toBe(expectedBusinessTypes.length)
  })
})
```

- [x] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/unit/detail-cards-registration.test.ts`
Expected: FAIL — 无法解析 `src/components/detail-cards`

- [x] **Step 3: 创建 index.ts 和第一批3个 DetailCard**

创建 `index.ts`：

```tsx
// src/components/detail-cards/index.ts
import { SupplierDetailCard } from './suppliers/SupplierDetailCard'
import { IncomeContractDetailCard } from './contracts/IncomeContractDetailCard'
import { ExpenseContractDetailCard } from './contracts/ExpenseContractDetailCard'

export const DETAIL_CARD_MAP: Record<string, React.ComponentType<{ data: any }>> = {
  supplier: SupplierDetailCard,
  income_contract: IncomeContractDetailCard,
  expense_contract: ExpenseContractDetailCard,
}

export { DetailGrid } from './DetailGrid'
export { SupplierDetailCard } from './suppliers/SupplierDetailCard'
export { IncomeContractDetailCard } from './contracts/IncomeContractDetailCard'
export { ExpenseContractDetailCard } from './contracts/ExpenseContractDetailCard'
```

创建 `SupplierDetailCard.tsx`（从 approvals/page.tsx L133-146 提取）：

```tsx
// src/components/detail-cards/suppliers/SupplierDetailCard.tsx
import { DetailGrid } from '../DetailGrid'

interface Props {
  data: {
    name: string
    supplierType: string
    contactPerson: string | null
    phone: string | null
    email: string | null
    address: string | null
    bankName: string | null
    bankAccount: string | null
    status: string
  }
}

export function SupplierDetailCard({ data }: Props) {
  const fields = [
    { label: '供应商名称', value: data.name },
    { label: '供应商性质', value: data.supplierType },
    { label: '联系人', value: data.contactPerson },
    { label: '联系电话', value: data.phone },
    { label: '邮箱', value: data.email },
    { label: '地址', value: data.address },
    { label: '开户银行', value: data.bankName },
    { label: '银行账号', value: data.bankAccount },
    { label: '状态', value: data.status },
  ]
  return <DetailGrid fields={fields} />
}
```

创建 `IncomeContractDetailCard.tsx`（从 approvals/page.tsx L267-321 提取，含分期付款子表）。

创建 `ExpenseContractDetailCard.tsx`（从 approvals/page.tsx L323-375 提取，含合同明细子表）。

> 注意：这两个有子表的组件代码较长，实际创建时从原文件逐字提取。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/unit/detail-cards-registration.test.ts`
Expected: PASS（但仅3个注册，预期18个，测试会失败）

- [ ] **Step 5: 继续添加剩余15个 DetailCard 的映射，直至测试通过**

Run: `npx vitest run test/unit/detail-cards-registration.test.ts`
Expected: PASS（全部18个注册完毕）

- [ ] **Step 6: 提交**

```bash
git add test/unit/detail-cards-registration.test.ts src/components/detail-cards/
git commit -m "feat: 创建 DETAIL_CARD_MAP 注册中心和18个 DetailCard 组件"
```

---

### Task 3: 创建 DetailCard 渲染测试（TDD — 选3个代表性组件）

**Files:**
- Test: `test/unit/detail-cards-render.test.tsx`
- Modify: （在 Task 2 中已创建，本任务只验证）

- [ ] **Step 1: 编写渲染测试**

选3个有代表性的组件：无子表（供应商）、有子表（收入合同）、复杂结构（工资发放）

```tsx
// test/unit/detail-cards-render.test.tsx
// @vitest-environment jsdom
import { describe, it, expect, afterEach } from 'vitest'
import { render, screen, cleanup } from '@testing-library/react'
import { SupplierDetailCard } from '../../src/components/detail-cards/suppliers/SupplierDetailCard'

afterEach(() => { cleanup() })

describe('SupplierDetailCard', () => {
  const mockData = {
    name: '测试供应商',
    supplierType: '一般纳税人',
    contactPerson: '李四',
    phone: '13900139000',
    email: 'lisi@test.com',
    address: '北京市朝阳区',
    bankName: '中国银行',
    bankAccount: '6222021234567890',
    status: 'ACTIVE',
  }

  it('渲染所有字段的 label', () => {
    render(<SupplierDetailCard data={mockData} />)
    expect(screen.getByText('供应商名称')).toBeDefined()
    expect(screen.getByText('供应商性质')).toBeDefined()
    expect(screen.getByText('联系人')).toBeDefined()
    expect(screen.getByText('联系电话')).toBeDefined()
    expect(screen.getByText('邮箱')).toBeDefined()
    expect(screen.getByText('地址')).toBeDefined()
    expect(screen.getByText('开户银行')).toBeDefined()
    expect(screen.getByText('银行账号')).toBeDefined()
    expect(screen.getByText('状态')).toBeDefined()
  })

  it('渲染所有字段的 value', () => {
    render(<SupplierDetailCard data={mockData} />)
    expect(screen.getByText('测试供应商')).toBeDefined()
    expect(screen.getByText('一般纳税人')).toBeDefined()
    expect(screen.getByText('李四')).toBeDefined()
    expect(screen.getByText('13900139000')).toBeDefined()
    expect(screen.getByText('lisi@test.com')).toBeDefined()
    expect(screen.getByText('北京市朝阳区')).toBeDefined()
    expect(screen.getByText('中国银行')).toBeDefined()
    expect(screen.getByText('6222021234567890')).toBeDefined()
    expect(screen.getByText('ACTIVE')).toBeDefined()
  })

  it('null 字段渲染为占位符', () => {
    render(<SupplierDetailCard data={{ ...mockData, contactPerson: null, phone: null }} />)
    const placeholders = screen.getAllByText('-')
    expect(placeholders.length).toBeGreaterThanOrEqual(2)
  })
})
```

> 注：IncomeContractDetailCard 和 SalaryPaymentDetailCard 的渲染测试类似模式，在 Task 6 批量提取后补充。

- [ ] **Step 2: 运行测试确认通过**

Run: `npx vitest run test/unit/detail-cards-render.test.tsx`
Expected: PASS（Task 2 已创建 SupplierDetailCard）

- [ ] **Step 3: 提交**

```bash
git add test/unit/detail-cards-render.test.tsx
git commit -m "test: 添加 DetailCard 渲染测试"
```

---

### Task 4: 供应商模块页面改用共享 DetailCard

**Files:**
- Modify: `src/app/(dashboard)/business/suppliers/page.tsx`
- 验证运行: `npx vitest run test/unit/`

- [ ] **Step 1: 修改供应商模块的详情弹窗**

找到 `suppliers/page.tsx` 中详情 `Modal` 内的字段渲染代码（约 L1127-1172），替换为：

```tsx
import { SupplierDetailCard } from '@/components/detail-cards'

// 在 Modal 中：
<Modal isOpen={!!detailSupplier} onClose={() => setDetailSupplier(null)} title="供应商详情">
  <SupplierDetailCard data={detailSupplier} />
  <div className="flex justify-end gap-3 pt-6 border-t border-gray-100 mt-6">
    {/* 操作按钮保持不变：编辑、删除等 */}
  </div>
</Modal>
```

- [x] **Step 2: 运行单元测试确认无回归**

Run: `npx vitest run test/unit/`
Expected: PASS

- [x] **Step 3: 提交**

```bash
git add src/app/(dashboard)/business/suppliers/page.tsx
git commit -m "refactor: 供应商详情弹窗改用共享 SupplierDetailCard"
```

---

### Task 5: 收入合同/支出合同模块页面改用共享 DetailCard

**Files:**
- Modify: `src/app/(dashboard)/contracts/income/page.tsx`
- Modify: `src/app/(dashboard)/contracts/expense/page.tsx`

- [ ] **Step 1: 修改收入合同详情弹窗**

替换 `income/page.tsx` 中详情 Modal 内的字段渲染代码为 `IncomeContractDetailCard`。

- [x] **Step 2: 修改支出合同详情弹窗**

替换 `expense/page.tsx` 中详情 Modal 内的字段渲染代码为 `ExpenseContractDetailCard`。

- [ ] **Step 3: 运行单元测试确认无回归**

Run: `npx vitest run test/unit/`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/app/(dashboard)/contracts/income/page.tsx src/app/(dashboard)/contracts/expense/page.tsx
git commit -m "refactor: 收入/支出合同详情弹窗改用共享 DetailCard"
```

---

### Task 6: 批量提取剩余15个 DetailCard

**Files:**
- Create: `src/components/detail-cards/` 下的剩余15个组件（按目录结构）
- Modify: `src/components/detail-cards/index.ts`（补充映射）

各组件内容直接从 `approvals/page.tsx` 逐字提取，对应关系：

| 组件 | 原文件行号 | 备注 |
|------|-----------|------|
| `business/QuotationDetailCard.tsx` | L148-157 | 无子表 |
| `business/OutsourcingDetailCard.tsx` | L159-173 | 无子表 |
| `business/PurchaseRequestDetailCard.tsx` | L175-217 | 含物资明细子表 |
| `business/DeliveryReceiptDetailCard.tsx` | L219-265 | 含物资明细子表 |
| `business/InquiryDetailCard.tsx` | L651-695 | 含物资明细子表 |
| `finance/NonContractIncomeDetailCard.tsx` | L377-395 | 无子表 |
| `finance/NonContractExpenseDetailCard.tsx` | L397-417 | 无子表 |
| `finance/PaymentApplicationDetailCard.tsx` | L419-439 | 无子表 |
| `finance/ExpenseReportDetailCard.tsx` | L441-482 | 含报销明细子表 |
| `finance/LendingOutDetailCard.tsx` | L484-522 | 含归还记录子表 |
| `finance/SalaryPaymentDetailCard.tsx` | L524-623 | 复杂（汇总+代扣代缴+员工明细） |
| `finance/BorrowingReturnDetailCard.tsx` | L625-636 | 无子表 |
| `finance/OtherBorrowingDetailCard.tsx` | L638-649 | 无子表 |
| `contracts/InterOrgContractDetailCard.tsx` | L697-723 | 无子表 |
| `contracts/ContractChangeOrderDetailCard.tsx` | L725-757 | 无子表 |

- [ ] **Step 1: 创建所有15个组件文件，内容从 approvals/page.tsx 提取**
- [ ] **Step 2: 更新 index.ts 注册所有映射**
- [ ] **Step 3: 运行注册完整性测试确认全部注册**

Run: `npx vitest run test/unit/detail-cards-registration.test.ts`
Expected: PASS（全部18个）

- [ ] **Step 4: 运行全量单元测试**

Run: `npx vitest run test/unit/`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/components/detail-cards/ test/unit/
git commit -m "feat: 批量提取剩余15个 DetailCard 组件"
```

---

### Task 7: 审批页面改用共享组件

**Files:**
- Modify: `src/app/(dashboard)/approvals/page.tsx`

- [ ] **Step 1: 删除内联的18个 DetailCard 组件定义**

删除从 `SupplierDetailCard` 定义开始到 `DETAIL_CARD_MAP` 结束的整个代码块（约 L133-L791）。

- [ ] **Step 2: 添加共享引用**

```tsx
import { DETAIL_CARD_MAP, DetailGrid } from '@/components/detail-cards'
```

确保 `BusinessDetailPanel` 中的动态查找代码不变：

```tsx
const CardComponent = DETAIL_CARD_MAP[businessType]
```

- [x] **Step 3: 运行构建验证**

Run: `npx next build`
Expected: BUILD SUCCESS

- [x] **Step 4: 运行全量测试**

Run: `npx vitest run test/unit/`
Expected: PASS

- [x] **Step 5: 提交**

```bash
git add src/app/(dashboard)/approvals/page.tsx
git commit -m "refactor: 审批页面改用共享 DETAIL_CARD_MAP"
```

---

### Task 8: 剩余模块页面改用共享 DetailCard

**Files:**
- Modify: `src/app/(dashboard)/contracts/internal-settlement/page.tsx`
- Modify: `src/app/(dashboard)/contracts/change-orders/page.tsx`
- 验证: `test/unit/route-integrity.test.ts`

- [ ] **Step 1: 修改内部结算模块详情弹窗** — 改用 `InterOrgContractDetailCard`
- [ ] **Step 2: 修改合同变更模块详情弹窗** — 改用 `ContractChangeOrderDetailCard`
- [ ] **Step 3: 运行路由完整性测试**

Run: `npx vitest run test/unit/route-integrity.test.ts`
Expected: PASS

- [x] **Step 4: 运行全量测试**

Run: `npx vitest run test/unit/`
Expected: PASS

- [x] **Step 5: 提交**

```bash
git add src/app/(dashboard)/contracts/internal-settlement/page.tsx src/app/(dashboard)/contracts/change-orders/page.tsx
git commit -m "refactor: 内部结算和合同变更详情改用共享 DetailCard"
```

---

### Task 9: 更新项目规则

**Files:**
- Modify: `.trae/rules/project_rules.md`

- [ ] **Step 1: 追加模块详情展示规范**

在 `project_rules.md` 的末尾追加：

```markdown
### 7. 模块详情展示规范

- 所有模块的详情展示组件位于 `src/components/detail-cards/` 下
- 模块页面和审批页面的详情弹窗**必须引用同一套共享 DetailCard 组件**
- 新增模块字段时，只需修改对应的 `XxxDetailCard.tsx` 文件（一个文件，两处生效）
- 新增流程模块时，必须：
  1. 在 `src/components/detail-cards/` 下创建 `XxxDetailCard.tsx`
  2. 在 `detail-cards/index.ts` 的 `DETAIL_CARD_MAP` 中注册
  3. 模块页面的详情弹窗引用该组件
  4. 审批页面自动通过 `DETAIL_CARD_MAP` 获取该组件
- 运行验证：`npx vitest run test/unit/detail-cards-registration.test.ts`（检查注册完整性）
```

- [ ] **Step 2: 提交**

```bash
git add .trae/rules/project_rules.md
git commit -m "docs: 追加模块详情展示规范到项目规则"
```

---

### Task 10: 回归验证

**Files:**
- 运行: `npx vitest run test/unit/`
- 运行: `npx next build`

- [ ] **Step 1: 运行全量单元测试**

Run: `npx vitest run test/unit/`
Expected: ALL PASS

- [ ] **Step 2: 运行构建验证**

Run: `npx next build`
Expected: BUILD SUCCESS
